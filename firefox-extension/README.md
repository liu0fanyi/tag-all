# tag-all Web Clipper

Firefox扩展，一键保存网页到tag-all云端数据库。

## 功能

- 🔖 点击工具栏图标保存当前页面
- 🏷️ 自动创建"web-bookmark" workspace
- 📝 自动添加"待整理"和"web-bookmark"标签  
- 📄 自动提取页面摘要
- 🔍 侧边栏搜索查看已保存内容

## 安装

### 方式1：临时加载（开发测试）

1. 打开Firefox，输入`about:debugging`
2. 点击"此Firefox"
3. 点击"临时载入附加组件"
4. 选择`manifest.json`文件

### 方式2：打包安装

```bash
cd firefox-extension
zip -r ../tag-all-clipper.xpi *
```

然后拖动xpi文件到Firefox安装。

## 配置

首次使用需要配置Turso数据库连接：

1. 右键点击扩展图标 → "选项"
2. 输入Turso Database URL（如：`lib sql://xxx.turso.io`）
3. 输入Auth Token
4. 点击"测试连接"确认配置正确
5. 点击"保存配置"

## 使用

### 保存网页
- 点击工具栏图标（默认会自动提取标题和摘要）
- 显示通知"已保存到云端！"

### 查看已保存内容
- `Ctrl+Shift+B` 或菜单栏 → 查看 → 侧边栏 → Web Bookmarks
- 侧边栏显示所有web-bookmark
- 输入框搜索
- 点击刷新按钮更新列表

## 技术栈

- Firefox WebExtension API (Manifest V2)
- @libsql/client (Turso JavaScript客户端)
- Mozilla Read ability (页面内容提取)

## 开发

### 目录结构
```
firefox-extension/
├── manifest.json          # 扩展配置
├── background.js          # 后台逻辑
├── content-extract.js     # 内容提取脚本
├── config/                # 配置页面
├── sidebar/               # 侧边栏
├── lib/                   # 第三方库
└── icons/                 # 图标资源
```

### 依赖

需要下载以下库到`lib/`目录：

1. `@libsql/client`: https://cdn.jsdelivr.net/npm/@libsql/client/+esm
2. `@mozilla/readability`: https://cdn.jsdelivr.net/npm/@mozilla/readability/+esm

## 故障排除

### 无法连接数据库
- 检查Turso URL和Token是否正确
- 确认网络连接正常
- 在配置页面点击"测试连接"

### 保存失败
- 打开浏览器控制台查看错误信息
- 确认tag-all桌面应用数据库结构已更新（包含url和summary字段）

## 许可证

MIT
